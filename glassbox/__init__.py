"""GlassBox — natural-language options strategist behind a deterministic governor.

Modules:
    glassbox.data_layer  — dumb, honest reporter of Alpaca broker state (Jhoosier lead).
    glassbox.screener    — chain screener, ``screen_chain()`` (teakeycee lead; lands separately).
"""
