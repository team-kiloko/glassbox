[pytest]
testpaths = tests
# Put the repo root on sys.path so `glassbox` imports without an install step.
# conftest.py's screener probe runs at import time, so this has to be config,
# not something a test arranges for itself.
pythonpath = .
